/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', 
    basePath: '/ProjektZaliczeniowy_Next', 
    images: {
      unoptimized: true, 
    },
};

module.exports = nextConfig;